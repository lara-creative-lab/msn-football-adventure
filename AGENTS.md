# MSN 球星大冒险项目规则

本文件适用于整个仓库。任何新任务开始时，不要依赖旧聊天记录；先读取以下文件，并以仓库当前状态为准：

1. `docs/MSN球星大冒险项目档案.md`
2. `docs/DECISIONS.md`
3. `docs/CURRENT_STATE.md`
4. `git status --short --branch`、`git log -5 --oneline` 和相关源码

## 信息优先级

发生冲突时按以下顺序处理：

1. 用户在当前任务中的最新明确指令
2. `docs/DECISIONS.md` 中已确认且未被撤销的决定
3. `docs/CURRENT_STATE.md` 中的当前状态与待办
4. `docs/MSN球星大冒险项目档案.md` 中的产品规格
5. 旧聊天、旧截图和历史实现

如果当前指令会推翻已确认决定，先指出冲突并取得用户确认；不要自行猜测。

## 修改原则

- 只修改当前需求涉及的功能，保留用户已经确认的玩法、角色关系和视觉资产。
- 不要重新实现已经存在且测试覆盖的功能。
- 重要参考图必须复制到 `docs/references/`，不要长期依赖系统临时目录或剪贴板路径。
- 视觉稿若被要求“先确认”，只能生成本地预览，未获确认前不得接入游戏或发布。
- 未经明确授权不得提交、推送或发布；获得发布授权后，仍需先完成验证。
- 每次完成重要需求后，同步更新 `docs/CURRENT_STATE.md`；若形成新的长期决定，同时更新 `docs/DECISIONS.md`。

## 验证与发布

- 基础验证：`npm test`
- 发布前检查：确认 `git diff`、当前分支、目标远程仓库和公开网址，避免推送到旧仓库。
- 本仓库受父级 `/Users/florafan/Documents/Codex/AGENTS.md` 约束：网站修改构建验证成功后，交付或发布前必须对待发布源码执行 Codex Security 标准扫描，并如实记录覆盖范围和结果。
- 如果安全扫描后源码又发生变化，发布前重新扫描新的源码快照。
- 发布完成后记录提交号、目标远程、部署结果和公开页面验证结果。
- 网络、凭据或托管平台失败时，明确说明失败层级；不要把“本地提交成功”表述成“线上发布成功”。

## 当前项目入口

- 源码目录：`public/`
- 主游戏脚本：`public/game-v33.js`
- 页面与样式：`public/index.html`、`public/style.css`
- 测试：`tests/site.test.mjs`
- 构建与测试：`npm run build`、`npm test`
